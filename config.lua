--- Configuration for opx77_input: how wide the modal is, and what it dims.

OPX_INPUT_CONFIG = {
  LOCALE = "en",
  WIDTH = 460, -- modal width in pixels, at a 1920-wide surface
  -- A scrim behind the modal. The player is typing and cannot move, so the scene
  -- underneath is not something they need to read.
  DIM = true,
}
